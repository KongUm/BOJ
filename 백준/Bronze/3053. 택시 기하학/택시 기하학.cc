#include <bits/stdc++.h>

#define ll long long
using namespace std;


signed main() {
    ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
    int r;
    double pi = 3.14159265359;
    cin >> r;
    cout.precision(16);
    cout << pi * r * r << "\n";
    cout.precision(16);
    cout << (double) r * r * 2;
}